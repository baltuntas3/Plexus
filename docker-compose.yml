services:
  mongo:
    image: mongo:7
    container_name: plexus-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: plexus

volumes:
  mongo-data:
